# Segredos / ambiente — NUNCA versionar
.env
.env.*
!.env.example
.meta-token
.app-secret
.user-token

# Mídia recebida (runtime) + sessão da Extranet (segredo) — NUNCA versionar.
# Evita que um `git add -A` arraste mídia pesada e o extranet-session.json.
uploads/

# HTML bruto do probe da Extranet (contém PII real — nomes/telefones de leads).
# A fixture ANONIMIZADA versionada é test/fixtures/extranet-leads-lista.html.
test/fixtures-probe-raw/

# Dependências
node_modules/

# Logs
*.log
npm-debug.log*

# Sistema operacional / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
